package controllers

import play.api._
import play.api.mvc._

object LandingController extends Controller {

  // TODO: Complete:
  //  - Check if the user is logged in:
  //     - If they are, redirect to the chat room
  //     - If they are, redirect to the login page
  //
  // NOTE: We don't know how to create HTML yet, so populate each response
  // with a plain text message.
  def index = Action { request =>
    ???
  }
}
